import 'package:flutter/material.dart';

// Definição das cores do app
class AppColors {
  static const primaryColor =
      Color(0xFFB71C1C); // Exemplo de cor primária do Flamengo
  static const accentColor = Color.fromARGB(255, 9, 7, 0);
  // Adicione mais cores conforme necessário
}

// Definição das strings do app
class AppStrings {
  static const appTitle = 'Flabeat';
  // Adicione mais strings conforme necessário
}

// Definição de estilos de texto reutilizáveis
class TextStyles {
  static const heading = TextStyle(
    fontSize: 24.0,
    fontWeight: FontWeight.bold,
    // Adicione mais propriedades conforme necessário
  );
  static const content = TextStyle(
    fontSize: 16.0,
    // Adicione mais propriedades conforme necessário
  );
  // Adicione mais estilos de texto conforme necessário
}
